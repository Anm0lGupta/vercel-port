// ============================================================
//  utils.js — shared helper functions
// ============================================================

/** Returns true when viewport width is ≤ 900px (mobile/tablet) */
export const isMobile = () => window.innerWidth <= 900;
